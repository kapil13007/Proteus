[pytest]
testpaths = tests
addopts = -p no:cacheprovider
filterwarnings =
    # Starlette 1.x nudges TestClient users towards httpx2; irrelevant for these tests
    ignore:Using `httpx` with `starlette.testclient`
